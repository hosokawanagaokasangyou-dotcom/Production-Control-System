---
description: コード変更直後に git commit と push を毎回実施する
alwaysApply: true
---

# Git: 変更直後の commit / push

## 必須動作

- ユーザー依頼に応じて**ワークスペースのコードや設定を変更した場合**、その作業単位の完了時（応答を終える前）に **`git commit` と `git push`** を実行する。
- 「変更した直後に毎回」とは、タスクや編集セッションごとに**未コミットの変更を残さない**ことを意味する。別ターンに先送りしない。

## 手順

1. `git status` で変更を確認する。
2. 意図したファイルのみをステージする（`git add`）。一時ファイルや `.xlsm` のロックファイル（`~$`）などはコミットに含めない。
3. 変更内容が分かる**日本語または英語の一行メッセージ**で `git commit` する。
4. リモートがある場合は **`git push`** する。初回やブランチ未設定時は必要に応じて upstream を設定する。

## 例外

- ユーザーが明示的に「コミットしない」「push しない」と言った場合のみ従う。
- ネットワークや認証エラーで push が失敗した場合は、実施したこと・エラー内容・ユーザーが取れる対処を報告する。

# -*- coding: utf-8 -*-
"""
xlwings ????how Console???? RunPython ??????1/2?????????cmd.exe ???????€?

???
----
- Excel ?? **xlwings ???????**??????€??????? **Show Console** ???????????
- ?????????????? **?????????????.xlsm??????§??????€??????** ``python/`` **????**
  ?????: ``.../???????????/python/xlwings_console_runner.py``???????????€?
  ``import xlwings_console_runner`` ??????? ``sys.path`` ?? ``python`` ??????????????????????
  **VBA ???? ``runpy.run_path`` ????????????????????????????????**??????? ``RunPython`` ?????§???€?
  ???????? ``xlwings.conf.json`` ?? PYTHONPATH ?????
- VBA???xlwings ???? ON?????????????????::

    xlwings.RunPython "import os, runpy, xlwings as xw; wb=xw.Book.caller(); p=os.path.join(os.path.dirname(str(wb.fullname)), 'python', 'xlwings_console_runner.py'); ns=runpy.run_path(p); ns['run_stage1_for_xlwings']()"

  ?????? ``????1_????????` / ``????2_????????` ????? ``STAGE12_USE_XLWINGS_RUNPYTHON`` ?????????????????€?
  ???????????? ``log/stage_vba_exitcode.txt`` ??1?????????VBA ?? cmd ??????????????€???€?

logging ?? planning_core ?? ``StreamHandler(sys.stdout)`` ??????????????€?
print ??????? xlwings ???????????????????€?

????
----
- ???????????? ``???????????`` ??????????????€?`_apply_workbook_env_overrides`` ??
  ``import planning_core`` ?????? ``os.environ`` ???????????``workbook_env_bootstrap``???€?
- ``planning_core`` ?? import ??? TASK_INPUT_WORKBOOK ????????????????????
  ???????????????? **???? ``planning_core`` ?? sys.modules ?????????? import** ?????
- cmd ?????? ``task_extract_stage1.py`` ?? planning_core ?????? ``execution_log.txt`` ???????
  **??????????????** ``run_stage1_for_xlwings`` / ``run_stage2_for_xlwings`` ?? import ??????????????1?????????
  ??????????????????????????????VBA ?? LOG ?????????????????????????????????€?
- ``runpy.run_path`` ??????? **sys.path ??????????????????????python ??????€???????????** ????????€?
  ``import planning_core`` ?? ``ModuleNotFoundError`` ??????€?????????????? ``_ensure_this_python_dir_on_syspath`` ???????
"""
from __future__ import annotations

import logging
import os
import sys
import traceback
from datetime import datetime

STAGE_VBA_EXIT_CODE_FILE = "stage_vba_exitcode.txt"


def _ensure_this_python_dir_on_syspath() -> None:
    """planning_core ?????§????????????? sys.path ?????????????run_path ??????????€?"""
    here = os.path.dirname(os.path.abspath(__file__))
    here_n = os.path.normcase(here)
    for _p in sys.path:
        try:
            if os.path.normcase(os.path.abspath(_p)) == here_n:
                return
        except (OSError, ValueError):
            continue
    sys.path.insert(0, here)


_ensure_this_python_dir_on_syspath()


def _apply_workbook_env_overrides() -> None:
    """?????????????????????????? TASK_INPUT_WORKBOOK ?????????lanning_core ???????????"""
    try:
        import workbook_env_bootstrap as _wbe

        _wbe.apply_from_task_input_workbook()
    except Exception:
        pass


def _write_stage_vba_exit_code(code: int) -> None:
    """VBA ?? ReadStageVbaExitCodeFromFile ?????????1???????????"""
    try:
        logd = os.path.join(os.getcwd(), "log")
        os.makedirs(logd, exist_ok=True)
        p = os.path.join(logd, STAGE_VBA_EXIT_CODE_FILE)
        with open(p, "w", encoding="utf-8", newline="\n") as f:
            f.write(str(int(code)))
            f.flush()
            os.fsync(f.fileno())
    except OSError:
        pass


def _purge_planning_core_modules() -> None:
    for k in list(sys.modules.keys()):
        if k == "planning_core" or k.startswith("planning_core."):
            del sys.modules[k]


def _prepare_from_caller_book() -> str:
    import xlwings as xw

    wb = xw.Book.caller()
    path = os.path.abspath(str(wb.fullname))
    root = os.path.dirname(path)
    os.chdir(root)
    os.environ["TASK_INPUT_WORKBOOK"] = path
    return path


def _execution_log_path() -> str:
    return os.path.join(os.getcwd(), "log", "execution_log.txt")


def _append_execution_log_line(level: str, msg: str) -> None:
    """
    cmd ?????? task_extract_stage1 ??????€?lanning_core ?????? log ???????????VBA ?? LOG ??????????€???€?
    """
    path = _execution_log_path()
    ts = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    line = f"{ts} - {level} - {msg}\n"
    try:
        os.makedirs(os.path.dirname(path), exist_ok=True)
        with open(path, "a", encoding="utf-8-sig", newline="\n") as f:
            f.write(line)
            f.flush()
            try:
                os.fsync(f.fileno())
            except OSError:
                pass
    except OSError:
        return
    try:
        import xlwings_splash_log as xsl

        if xsl.enabled():
            xsl.append_formatted_line(line)
    except Exception:
        pass


def _append_execution_log_traceback(title: str) -> None:
    """except ??????????????????format_exc ?????????????€?"""
    _append_execution_log_line("ERROR", title)
    tb = traceback.format_exc()
    try:
        path = _execution_log_path()
        os.makedirs(os.path.dirname(path), exist_ok=True)
        with open(path, "a", encoding="utf-8-sig", newline="\n") as f:
            f.write(tb)
            if not tb.endswith("\n"):
                f.write("\n")
            f.flush()
            try:
                os.fsync(f.fileno())
            except OSError:
                pass
    except OSError:
        pass


def run_stage1_for_xlwings() -> int:
    """
    ????1???run_stage1_extract???€????€?: 0=???, 1=????, 2=caller ?????
    ??????? log/stage_vba_exitcode.txt ???????????????
    """
    rc = 1
    try:
        try:
            _prepare_from_caller_book()
        except Exception:
            logging.exception(
                "xlwings: Book.caller() ??????????????"
                " ?????????????????????? RunPython ???????????"
            )
            rc = 2
            return rc
        _apply_workbook_env_overrides()
        _append_execution_log_line(
            "INFO",
            "????1: xlwings run_stage1_for_xlwings ??????planning_core ????????????",
        )
        _purge_planning_core_modules()
        try:
            import planning_core as pc

            ok = pc.run_stage1_extract()
            rc = 0 if ok else 1
        except SystemExit as e:
            c = e.code
            if c is None:
                rc = 0
            elif isinstance(c, int):
                rc = 0 if c == 0 else c
            else:
                rc = 1
        except Exception:
            logging.exception("xlwings: ????1???????????")
            _append_execution_log_traceback("xlwings: ????1???????????")
            rc = 1
        return rc
    finally:
        _write_stage_vba_exit_code(rc)


def run_refresh_plan_input_dispatch_trial_order_for_xlwings() -> int:
    """
    ????_??????????????????refresh_plan_input_dispatch_trial_order ???????
    VBA: XwRunConsoleRunner "run_refresh_plan_input_dispatch_trial_order_for_xlwings"
    """
    rc = 1
    try:
        try:
            _prepare_from_caller_book()
        except Exception:
            logging.exception("xlwings: Book.caller() ????????????")
            rc = 2
            return rc
        _apply_workbook_env_overrides()
        _append_execution_log_line(
            "INFO",
            "???????: xlwings run_refresh_plan_input_dispatch_trial_order_for_xlwings ????",
        )
        _purge_planning_core_modules()
        try:
            import planning_core as pc

            ok = pc.refresh_plan_input_dispatch_trial_order_only()
            rc = 0 if ok else 1
        except SystemExit as e:
            c = e.code
            if c is None:
                rc = 0
            elif isinstance(c, int):
                rc = 0 if c == 0 else c
            else:
                rc = 1
        except Exception:
            logging.exception("xlwings: ?????????????")
            _append_execution_log_traceback("xlwings: ?????????????")
            rc = 1
        return rc
    finally:
        _write_stage_vba_exit_code(rc)


def run_sort_plan_input_dispatch_trial_order_by_float_keys_for_xlwings() -> int:
    """
    ????_?????: ???????????????? 1..n?xlwings ???
    VBA: XwRunConsoleRunner "run_sort_plan_input_dispatch_trial_order_by_float_keys_for_xlwings"
    """
    rc = 1
    try:
        try:
            _prepare_from_caller_book()
        except Exception:
            logging.exception("xlwings: Book.caller() ?????")
            rc = 2
            return rc
        _apply_workbook_env_overrides()
        _append_execution_log_line(
            "INFO",
            "?????: xlwings run_sort_plan_input_dispatch_trial_order_by_float_keys_for_xlwings ??",
        )
        _purge_planning_core_modules()
        try:
            import planning_core as pc

            ok = pc.sort_plan_input_dispatch_trial_order_by_float_keys_only()
            rc = 0 if ok else 1
        except SystemExit as e:
            c = e.code
            if c is None:
                rc = 0
            elif isinstance(c, int):
                rc = 0 if c == 0 else c
            else:
                rc = 1
        except Exception:
            logging.exception("xlwings: ????????????????")
            _append_execution_log_traceback("xlwings: ????????????????")
            rc = 1
        return rc
    finally:
        _write_stage_vba_exit_code(rc)


def run_stage2_for_xlwings() -> int:
    """
    ????2???generate_plan???€????€?: 0=????????, 1=????, 2=caller ?????
    ??????? log/stage_vba_exitcode.txt ???????????????
    """
    rc = 1
    try:
        try:
            _prepare_from_caller_book()
        except Exception:
            logging.exception("xlwings: Book.caller() ??????????????")
            rc = 2
            return rc
        _apply_workbook_env_overrides()
        _append_execution_log_line(
            "INFO",
            "????2: xlwings run_stage2_for_xlwings ??????planning_core ????????????",
        )
        _purge_planning_core_modules()
        try:
            import planning_core as pc

            pc.generate_plan()
            rc = 0
        except SystemExit as e:
            c = e.code
            if c is None:
                rc = 0
            elif isinstance(c, int):
                rc = 0 if c == 0 else c
            else:
                rc = 1
        except Exception:
            logging.exception("xlwings: ????2???????????")
            _append_execution_log_traceback("xlwings: ????2???????????")
            rc = 1
        return rc
    finally:
        _write_stage_vba_exit_code(rc)

# 配台ルール（現行仕様）

実装の正は `python/planning_core.py` の `generate_plan`・`build_task_queue_from_planning_df`・日次割付ループおよび周辺関数です。本書はその挙動を業務向けに要約したものです。

---

## A. 全加工依頼に適用する追加ルール

### A-1 「加工内容」列に基づく工程順序（同一依頼NO）

**データ**: マクロブック **「加工計画DATA」** および段階1経由の **「配台計画_タスク入力」** に列 **`加工内容`** がある場合に有効です（段階1で `SOURCE_BASE_COLUMNS` に含まれるため、抽出データに引き継がれます）。

**ルール**:

- 同一 **依頼NO** で複数行（複数工程）があり、いずれも **配台不要ではない** とき、**加工内容** の文字列を **カンマ `,` 区切り**で分割した順序が **工程の正しい順序**です（先頭が最初の工程）。
- 例: `EC,検査` → **EC の残ロールがすべて 0 になるまで、検査行には割付しません**（検査はキューに残ります）。
- 各行の **工程名** が分割後のどの要素と一致するかで順序番号を決めます（NFKC・空白除去で照合）。**加工内容に現れない工程名**の行には、この順序制約はかかりません。
- **配台不要**の行はキューに入らないため、ブロッカーにもなりません。

**実装上の補足**: 同一日内は、残量のあるタスクを **加工内容順 → 結果シート用ソートキー**の順で何度も回し、1 回のループで各タスクへ **最大1チャンク**だけ割り当てます（前工程と後工程を同日内で交互に進められます）。

---

## B. 個別に指定する追加ルール

### B-1 EC（EC機　湖南）→ 検査（熱融着機　湖南）のロールパイプライン

**対象**（コード定数・依頼NOが同じ行同士のみ）:

| 段階 | 工程名 | 機械名 |
|------|--------|--------|
| 先行 | `EC` | `EC機　湖南` |
| 後続 | `検査` | `熱融着機　湖南` |

**ルール**:

- **ロール単位**でパイプラインします。EC 側で **完了したロール数**を `ec_done`、検査側で **完了したロール数**を `insp_done` とします（計画上の残量から算出）。
- **段取りバッファ**: **EC が 2 ロール完了するまで、検査には 1 ロールも割り当てません**（`ROLL_PIPELINE_INITIAL_BUFFER_ROLLS = 2`）。
- バッファ後は、常に **検査済ロール数** `insp_done` が次の上限を超えないようにします。  
  **上限**: `max(0, ec_done - 2 + 1)`（= `ec_done - 1`、ただし `ec_done < 2` の間は実質 0）。  
  解釈: EC が 2 ロール終わった時点で検査 1 ロール目が許可され、その後は **EC で 1 ロール進むごとに、検査でも最大 1 ロール分**進められます（並行のずらし積み）。
- 上記は **A-1 の工程順**（EC が検査より前）と整合します。他の依頼NOや工程の組み合わせには適用されません。

**定数の変更**: `planning_core.py` 内の `ROLL_PIPELINE_*` および `ROLL_PIPELINE_INITIAL_BUFFER_ROLLS` を編集してください。

---

## 1. 配台の対象外（キューに載せない）

- **完了扱い**の行（完了キーワード等で除外）
- 配台計画シートの **「配台不要」** がオン
- **同一依頼NO × 同一機械名**で複数行あるグループのうち、工程名が **「分割」** の行は、セルが空なら自動で配台不要
- マクロブック **「設定_配台不要工程」**  
  - C列＝常に配台不要の工程×機械  
  - または E列の条件（AI が JSON 化したロジック）が真のとき、その組み合わせの行は配台不要

---

## 2. タスクの並び順（割付の試行順）

`task_queue` をソートした順に、日ごとに割付を試します（先に並んだタスクほど先に機械・人員を取りに行く）。

比較キーは概ね次の順です（先頭ほど優先）。

1. **納期・目標日のソース**（`due_source_rank` が小さいほど先）  
   - 指定納期_上書き（セル）  
   - 特別指定備考 AI の目標完了日等  
   - 特別指定備考 AI の開始日  
   - 回答納期  
   - 指定納期
2. **完了日指定系**（AI で締めを上書きしたもの）を先
3. **加工途中**（実績あり・`in_progress`）を先
4. **`priority` が小さい**ほど先（セル優先、無ければ特別指定備考 AI）
5. **納期が「今日以前」扱い**（`due_urgent`）を先
6. **`due_basis_date` が早い**ほど先
7. **`start_date_req` が早い**ほど先
8. **依頼NO**のタイブレーク（ハイフン後の数値が小さい方が先）
9. **必要OP人数が大きい**ほど先（`need` 解決後の値）

### 2.1 開始日 `start_date_req`

- 既定：**原反投入日の翌日**（原反投入日が無い場合は計画基準日＝データ抽出日）
- 特別指定（セル／特別指定備考 AI）に開始日があればそれを採用（原反投入日と同日開始もあり得る）
- メイン **再優先特別記載** で指定した **工場休業日** は全日非稼働扱い
- 開始日が **誰も出勤していない日** のとき、**直前の稼働日**へ繰り上げ

### 2.2 原反投入日と同日開始

- 原反投入日と同日に開始し、かつ特別な開始日上書きが無い場合、開始は **10:30 以降**（`same_day_raw_start_limit`）

---

## 3. 誰を候補にするか（スキル・人数）

### 3.1 稼働

- `master.xlsm` 各メンバー勤怠で **`is_working`** の人のみがその日の候補プールに入る
- 勤怠 **備考** に **「配台不可」**（空白の有無・全半角の違いは正規化して判定）や **「配台NG」** が含まれる日は、その日は **加工配台の候補に入れない**（`is_working` と同等の扱い）。勤怠備考 AI が `is_holiday: false` を返しても **備考の明示が優先**される。
- 個人の空き開始は **`start_dt`**（退勤は `end_dt`）

### 3.2 スキル（`skills` シート）

- **工程名＋機械名**の交差セルが **OP** または **AS**（＋優先度整数）のメンバーのみ候補
- チームは **少なくとも1名が OP** でなければ不採用（AS のみの組は不可）
- 優先度は **数値が小さいほど**チーム選定で有利

### 3.3 必要人数 `req_num`

1. 配台計画シートの **必要OP** 列に正の整数があればそれを使用
2. 無ければ **`need` シート**：`工程+機械名` → 機械名のみ → 工程名のみの順で参照
3. **特別指定1〜99** は、条件が依頼NOに一致したうえで上書き（**order が小さいほど優先**）

### 3.4 メイン「再優先特別記載」（グローバル AI）

- **`ignore_need_minimum`** … `req_num` を **1** に固定（`need`・行の必要OPより優先）
- **`ignore_skill_requirements`** … 全員 **OP 扱い**かつ優先度を実質同一にし、スキルマトリクスを無視
- **`abolish_all_scheduling_limits`** … 設備専有・原反同日10:30・指定開始時刻・マクロ実行時刻下限などを適用しない（併せて skill / need も緩和される経路あり）

### 3.5 余剰増員（`need` の「配台時追加人数」行）

- 工程×機械ごとに、**必要人数を満たしたうえで追加で入れられる人数の上限**（`extra_max`）
- チーム人数は **`req_num` から `min(req_num + extra_max, 候補数)`** まで探索

---

## 4. 時刻・設備の制約

### 4.1 チーム開始時刻

- 各メンバーの **`avail_dt`（その時点の空き）** の **最大値**（最も遅い人に合わせる）
- **`abolish_all_scheduling_limits` がオフ**のとき、次を順に適用  
  - **同一設備**は1タスクずつ：`machine_avail_dt` より前には開始しない  
  - 原反同日の **10:30 下限**  
  - タスクの **earliest_start_time**（セル／AI）  
  - 計画基準日が **実行日と同じ日** のとき、**データ抽出日時（マクロ基準時刻）より前**には開始しない

### 4.2 休憩・中抜け

- **`breaks_dt`**（所定休憩＋中抜け）：勤怠備考 AI の中抜け、または AI が空のときは `infer_mid_break_from_reason` による推定を `breaks_dt` に載せ、**`calculate_end_time`** でその日処理可能な単位数を算出します。

### 4.3 増員時の作業速度

- 必要人数を超えた人数について、単位あたり時間に短縮係数 **`_surplus_team_time_factor`** を掛ける
- 短縮の上限は定数 **`SURPLUS_TEAM_MAX_SPEEDUP_RATIO`**（既定 **0.05**＝最大約5%）の線形モデル

### 4.4 標準シフト枠（参照）

- **`DEFAULT_START_TIME`** … 8:45  
- **`DEFAULT_END_TIME`** … 17:00  

---

## 5. チーム選定の優先基準

候補チームごとにタプルを比較し、**辞書式で小さい方**を採用します。

環境変数 **`TEAM_ASSIGN_PRIORITIZE_SURPLUS_STAFF`**（既定 `"1"`）がオンのとき:

- **`(-チーム人数, 開始時刻, -その日の単位数, 優先度合計)`**（辞書式で小さい方が勝ち）  
  → **人数が多い**ほど優先 → **開始が早い** → **その日の処理量が多い**

オフのとき:

- **`(開始時刻, -その日の単位数, 優先度合計)`**

### 5.1 主担当（lead OP）

- **担当OP指名**が当日の OP 候補に解決でき、かつチームに含まれる → その人を lead
- それ以外 → OP のうち **(スキル優先度, 名前)** が最小の人

### 5.2 担当OP指名の入力源と照合

- 配台計画の **担当OP_指定** と、特別指定備考 AI の **`preferred_operator`**（セル優先）
- メイン再優先の **`task_preferred_operators`** がある場合は **最優先で上書き**
- 名前は NFKC 正規化・**富田/冨田**（姓側の表記寄せ）で、当日 **`op_today`**（その設備で OP の人）と照合
- **姓・名の表記ゆれルール**（`skills` の全メンバーで判定）  
  - **姓**は正規化後 **完全一致のみ**（別姓を名のゆれで誤マッチさせない）。  
  - **`skills` に同一姓のメンバーが2人以上いない** ときだけ **名**は、完全一致に加え **どちらかの文字列が他方を含む**場合も一致とみなす（略称・表記のゆれ）。  
  - **同一姓がロスターにいる** ときは **名も完全一致**のみ。  
  - 姓・名の区切りは **最初の半角／全角空白**（マスタの「姓　名」形式に合わせる）。名ゆれモードで **姓だけ**入力したとき、同姓候補が複数いれば **解決しない**（曖昧のため）。
- **一致しない場合**はログに記録し、**指名は無効**となり、通常の組み合わせ探索のみ（別の OP で埋まり得る）

---

## 6. 速度調整（グローバル）

- メイン再優先の **`global_speed_rules`** により、工程×機械に **速度倍率**を掛けられる（`speed` への累積適用）

---

## 7. 計画の日付範囲

- 勤怠データに含まれる日のうち、**計画基準日（データ抽出日）以降**が対象

---

## 8. 関連ファイル・定数（開発用）

| 項目 | 主な参照先 |
|------|------------|
| 本体ロジック | `python/planning_core.py` |
| スキル・need | `master.xlsm` … `skills`, `need` |
| タスク | 配台ブック … `配台計画_タスク入力` |
| 配台不要マスタ | `設定_配台不要工程` |
| 余剰優先 | 環境変数 `TEAM_ASSIGN_PRIORITIZE_SURPLUS_STAFF` |
| 工程順（加工内容） | 列 `加工内容`・`_collect_process_content_order_by_task_id` ほか |
| EC→検査パイプライン | `ROLL_PIPELINE_*`・`ROLL_PIPELINE_INITIAL_BUFFER_ROLLS` |
| 増員短縮上限率 | `SURPLUS_TEAM_MAX_SPEEDUP_RATIO`（`planning_core.py` 先頭付近） |

---

*最終更新: 実装に追随するため、ルール変更時は `planning_core.py` を確認し本ファイルを更新してください。*

# モジュール分割バージョン（参照用）

このフォルダ内の `.bas` / `.frm` / `.cls` / `.txt` は **リポジトリ上の閲覧・差分・バックアップ用の分割コピー**であり、**実行時ロジックの正本ではありません**。Python／xlwings からこのパスを直接 import する構成にもしていません。

実際に動く VBA は **master.xlsm 等のブック内モジュール**が正です。修正は VBE 側で行い、必要ならここへ内容を手動で反映してください。
